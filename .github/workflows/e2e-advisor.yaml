# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

name: E2E / Advisor

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  # Fork PRs cannot access advisor secrets in pull_request context. Wait for
  # normal CI, then analyze the fork head as inert data from this trusted
  # main-branch workflow.
  workflow_run:
    workflows: ["CI / Pull Request"]
    types: [completed]
  workflow_dispatch:
    inputs:
      base_ref:
        description: Base ref to diff against
        required: false
        default: origin/main
      head_ref:
        description: Head ref to diff
        required: false
        default: HEAD
      target_repo:
        description: Optional repo to analyze, e.g. NVIDIA/NemoClaw
        required: false
        type: string
        default: ""
      target_pr:
        description: Optional pull request number in target_repo to analyze
        required: false
        type: string
        default: ""
      target_base:
        description: Base branch to use with target_repo/target_pr manual analysis
        required: false
        type: string
        default: main
      run_analysis:
        description: Run E2E recommendation analysis
        required: false
        type: boolean
        default: true

permissions:
  contents: read
  # `pull-requests: write` is required for the advisor to post (or update) a
  # comment on a PR via POST /repos/:o/:r/issues/:n/comments. Even though the
  # endpoint lives under `/issues/`, for GITHUB_TOKEN the permission that
  # actually gates PR comments is `pull-requests`, not `issues`. With only
  # `pull-requests: read`, the endpoint returns 403 "Resource not accessible
  # by integration" despite `issues: write`. See the comment step below and
  # https://github.com/orgs/community/discussions/56632.
  pull-requests: write
  issues: write

concurrency:
  group: e2e-advisor-${{ github.event.pull_request.number || github.event.workflow_run.head_repository.full_name || github.repository }}-${{ github.event.workflow_run.head_branch || github.ref }}
  cancel-in-progress: true

jobs:
  advise:
    name: E2E recommendation
    if: ${{ github.repository == 'NVIDIA/NemoClaw' && (github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == 'NVIDIA/NemoClaw') || (github.event_name == 'workflow_run' && github.event.workflow_run.event == 'pull_request' && github.event.workflow_run.head_repository.full_name != 'NVIDIA/NemoClaw')) }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      # Pin the Pi SDK to a known-good version. Updates should go through
      # the normal dependency-review path (e.g. Dependabot) so a compromised
      # upstream release cannot execute automatically in this secret-bearing job.
      PI_SDK_VERSION: "0.74.0"
      # Keep the advisor timeout shorter than the GitHub job timeout so the
      # analyzer can write failure artifacts, summaries, and PR comments.
      E2E_ADVISOR_TIMEOUT_MS: "900000"
      E2E_ADVISOR_HEARTBEAT_MS: "60000"
      # The trusted checkout always lives at this path and is the only source
      # of advisor implementation code executed in this job. The PR content,
      # if any, is mounted as read-only analysis data under PR_WORKDIR.
      ADVISOR_DIR: ${{ github.workspace }}/advisor
    steps:
      # Trusted-code boundary: the advisor implementation (analyze.mts,
      # targets.mts, comment.mts, target-comment.mts, schema.json, and
      # targets-schema.json) is always fetched
      # from the main branch of this repo, regardless of what the PR changed.
      # This prevents a future PR from modifying advisor code and executing
      # arbitrary Node in a job that holds API keys and a write-scoped token.
      - name: Checkout trusted advisor code (main)
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          repository: NVIDIA/NemoClaw
          ref: main
          path: advisor
          persist-credentials: false

      - name: Resolve fork PR from completed CI head
        id: fork_pr
        if: ${{ github.event_name == 'workflow_run' }}
        env:
          GH_TOKEN: ${{ github.token }}
          HEAD_REPO: ${{ github.event.workflow_run.head_repository.full_name }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          set -euo pipefail

          if [[ ! "$HEAD_REPO" =~ ^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$ ]] ||
             [[ ! "$HEAD_SHA" =~ ^[0-9a-f]{40}$ ]]; then
            echo "::error::workflow_run supplied an invalid fork repository or head SHA"
            exit 1
          fi

          pulls="$RUNNER_TEMP/fork-prs.json"
          gh api --paginate --slurp -X GET \
            -H "Accept: application/vnd.github+json" \
            "/repos/$GITHUB_REPOSITORY/pulls" \
            -f state=open \
            -f per_page=100 > "$pulls"

          count=$(jq --arg sha "$HEAD_SHA" --arg head_repo "$HEAD_REPO" --arg base_repo "$GITHUB_REPOSITORY" \
            '[.[][] | select(.head.sha == $sha and .head.repo.full_name == $head_repo and .base.repo.full_name == $base_repo)] | length' \
            "$pulls")
          if [ "$count" != "1" ]; then
            echo "::error::Expected exactly one open fork PR for $HEAD_REPO@$HEAD_SHA, found $count"
            exit 1
          fi

          pr_number=$(jq -r --arg sha "$HEAD_SHA" --arg head_repo "$HEAD_REPO" --arg base_repo "$GITHUB_REPOSITORY" \
            '.[][] | select(.head.sha == $sha and .head.repo.full_name == $head_repo and .base.repo.full_name == $base_repo) | .number' \
            "$pulls")
          base_ref=$(jq -r --arg sha "$HEAD_SHA" --arg head_repo "$HEAD_REPO" --arg base_repo "$GITHUB_REPOSITORY" \
            '.[][] | select(.head.sha == $sha and .head.repo.full_name == $head_repo and .base.repo.full_name == $base_repo) | .base.ref' \
            "$pulls")
          if [[ ! "$pr_number" =~ ^[1-9][0-9]*$ ]] ||
             [[ -z "$base_ref" || "$base_ref" == -* || "$base_ref" == /* || "$base_ref" == *..* || "$base_ref" == *:* || "$base_ref" =~ [[:space:]] || ! "$base_ref" =~ ^[A-Za-z0-9._/-]+$ ]]; then
            echo "::error::Resolved fork PR metadata is invalid"
            exit 1
          fi

          echo "pr_number=$pr_number" >> "$GITHUB_OUTPUT"
          echo "base_ref=$base_ref" >> "$GITHUB_OUTPUT"

      # PR content is checked out as inert analysis data only. Nothing from
      # this directory is invoked as code by the advisor scripts; it is read
      # via `git diff` / `fs.readFileSync` from inside the trusted scripts.
      - name: Checkout PR workspace (read-only data)
        if: ${{ github.event_name == 'pull_request' }}
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          path: pr-workdir
          fetch-depth: 0
          persist-credentials: false

      # For manual workflow_dispatch against this repo (no target_repo given),
      # check out the ref the maintainer dispatched against as the workdir.
      # workflow_dispatch requires write access, so this ref is trusted for
      # the purposes of analysis-data access, though we still never execute
      # it as code.
      - name: Checkout dispatch workspace (read-only data)
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.target_repo == '' }}
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          path: pr-workdir
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.0.0
        with:
          node-version: "22"

      - name: Set default advisor workdir
        run: echo "ADVISOR_WORKDIR=$GITHUB_WORKSPACE/pr-workdir" >> "$GITHUB_ENV"

      - name: Prepare target PR checkout
        if: ${{ github.event_name == 'workflow_run' || (github.event_name == 'workflow_dispatch' && inputs.target_repo != '' && inputs.target_pr != '') }}
        env:
          TARGET_REPO: ${{ github.event_name == 'workflow_run' && github.repository || inputs.target_repo }}
          TARGET_PR: ${{ github.event_name == 'workflow_run' && steps.fork_pr.outputs.pr_number || inputs.target_pr }}
          TARGET_BASE: ${{ github.event_name == 'workflow_run' && steps.fork_pr.outputs.base_ref || inputs.target_base }}
        run: |
          if [[ ! "$TARGET_REPO" =~ ^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$ ]]; then
            echo "::error::target_repo must match owner/repo with GitHub-safe characters"
            exit 1
          fi
          if [[ ! "$TARGET_PR" =~ ^[0-9]+$ ]]; then
            echo "::error::target_pr must be decimal digits"
            exit 1
          fi
          if [[ -z "$TARGET_BASE" || "$TARGET_BASE" == -* || "$TARGET_BASE" == /* || "$TARGET_BASE" == *..* || "$TARGET_BASE" == *:* || "$TARGET_BASE" =~ [[:space:]] || ! "$TARGET_BASE" =~ ^[A-Za-z0-9._/-]+$ ]]; then
            echo "::error::target_base must be a safe branch/ref token"
            exit 1
          fi
          TARGET_DIR=/tmp/e2e-advisor-target
          rm -rf "$TARGET_DIR"
          mkdir -p "$TARGET_DIR"
          git -C "$TARGET_DIR" init
          git -C "$TARGET_DIR" remote add target "https://github.com/${TARGET_REPO}.git"
          git -C "$TARGET_DIR" fetch --no-tags target "$TARGET_BASE"
          git -C "$TARGET_DIR" fetch --no-tags target "pull/${TARGET_PR}/head:refs/remotes/target/pr-${TARGET_PR}"
          git -C "$TARGET_DIR" checkout --detach "refs/remotes/target/pr-${TARGET_PR}"
          echo "ADVISOR_WORKDIR=$TARGET_DIR" >> "$GITHUB_ENV"
          echo "PR_NUMBER=$TARGET_PR" >> "$GITHUB_ENV"

      # Pinned SDK install. The version is held in PI_SDK_VERSION above so
      # the pin is reviewed as a code change, not silently inherited from
      # whatever @latest points to at runtime. Install into an isolated temp
      # prefix, then expose that dependency tree to the trusted advisor script.
      - name: Install Pi SDK
        run: |
          PI_SDK_DIR="$RUNNER_TEMP/pi-sdk"
          npm install --prefix "$PI_SDK_DIR" --ignore-scripts --no-save --package-lock=false --before=2026-05-14T00:00:00.000Z "@earendil-works/pi-coding-agent@${PI_SDK_VERSION}"
          rm -rf "$ADVISOR_DIR/node_modules"
          ln -s "$PI_SDK_DIR/node_modules" "$ADVISOR_DIR/node_modules"

      - name: Run E2E recommendation advisor
        id: analysis
        continue-on-error: true
        env:
          BASE_REF: ${{ github.event_name == 'pull_request' && format('origin/{0}', github.base_ref) || (github.event_name == 'workflow_run' && format('target/{0}', steps.fork_pr.outputs.base_ref) || (github.event_name == 'workflow_dispatch' && inputs.target_repo != '' && inputs.target_pr != '' && format('target/{0}', inputs.target_base) || inputs.base_ref)) }}
          HEAD_REF: ${{ (github.event_name == 'pull_request' || github.event_name == 'workflow_run') && 'HEAD' || (github.event_name == 'workflow_dispatch' && inputs.target_repo != '' && inputs.target_pr != '' && 'HEAD' || inputs.head_ref) }}
          E2E_ADVISOR_RUN_ANALYSIS: ${{ github.event_name == 'workflow_dispatch' && inputs.run_analysis == false && '0' || '1' }}
          # Preferred E2E advisor secret.
          E2E_ADVISOR_API_KEY: ${{ secrets.PI_E2E_ADVISOR_API_KEY }}
        run: |
          cd "$ADVISOR_WORKDIR"
          node --experimental-strip-types "$ADVISOR_DIR/tools/e2e-advisor/analyze.mts" \
            --base "$BASE_REF" \
            --head "$HEAD_REF" \
            --schema "$ADVISOR_DIR/tools/e2e-advisor/schema.json" \
            --out-dir "$GITHUB_WORKSPACE/artifacts/e2e-advisor"

      - name: Run E2E target advisor
        id: target-analysis
        continue-on-error: true
        env:
          BASE_REF: ${{ github.event_name == 'pull_request' && format('origin/{0}', github.base_ref) || (github.event_name == 'workflow_run' && format('target/{0}', steps.fork_pr.outputs.base_ref) || (github.event_name == 'workflow_dispatch' && inputs.target_repo != '' && inputs.target_pr != '' && format('target/{0}', inputs.target_base) || inputs.base_ref)) }}
          HEAD_REF: ${{ (github.event_name == 'pull_request' || github.event_name == 'workflow_run') && 'HEAD' || (github.event_name == 'workflow_dispatch' && inputs.target_repo != '' && inputs.target_pr != '' && 'HEAD' || inputs.head_ref) }}
          E2E_TARGET_ADVISOR_RUN_ANALYSIS: ${{ github.event_name == 'workflow_dispatch' && inputs.run_analysis == false && '0' || '1' }}
          # Reuse the shared E2E advisor secret. The target advisor is a
          # separate prompt/agent but uses the same model and credential.
          E2E_ADVISOR_API_KEY: ${{ secrets.PI_E2E_ADVISOR_API_KEY }}
        run: |
          cd "$ADVISOR_WORKDIR"
          node --experimental-strip-types "$ADVISOR_DIR/tools/e2e-advisor/targets.mts" \
            --base "$BASE_REF" \
            --head "$HEAD_REF" \
            --schema "$ADVISOR_DIR/tools/e2e-advisor/targets-schema.json" \
            --out-dir "$GITHUB_WORKSPACE/artifacts/e2e-advisor"

      - name: Publish job summary
        if: always()
        run: |
          if [ -f "$GITHUB_WORKSPACE/artifacts/e2e-advisor/e2e-advisor-summary.md" ]; then
            cat "$GITHUB_WORKSPACE/artifacts/e2e-advisor/e2e-advisor-summary.md" >> "$GITHUB_STEP_SUMMARY"
          else
            printf '# E2E Recommendation Advisor\n\nAdvisor analysis did not produce a summary. See raw artifacts/logs.\n' >> "$GITHUB_STEP_SUMMARY"
          fi
          if [ -f "$GITHUB_WORKSPACE/artifacts/e2e-advisor/e2e-target-advisor-summary.md" ]; then
            cat "$GITHUB_WORKSPACE/artifacts/e2e-advisor/e2e-target-advisor-summary.md" >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Post E2E advisor PR comment
        if: ${{ always() && (github.event_name == 'pull_request' || github.event_name == 'workflow_run') }}
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.E2E_ADVISOR_GITHUB_TOKEN || github.token }}
          PR_NUMBER: ${{ github.event_name == 'workflow_run' && steps.fork_pr.outputs.pr_number || github.event.pull_request.number }}
        run: |
          COMMENT_ARGS=(
            --repo "$GITHUB_REPOSITORY"
            --pr "$PR_NUMBER"
            --summary "$GITHUB_WORKSPACE/artifacts/e2e-advisor/e2e-advisor-summary.md"
            --result "$GITHUB_WORKSPACE/artifacts/e2e-advisor/e2e-advisor-final-result.json"
          )
          if [ -f "$ADVISOR_DIR/tools/e2e-advisor/comment.mts" ]; then
            node --experimental-strip-types "$ADVISOR_DIR/tools/e2e-advisor/comment.mts" "${COMMENT_ARGS[@]}"
          elif [ -f "$ADVISOR_DIR/tools/e2e-advisor/comment.mjs" ]; then
            node "$ADVISOR_DIR/tools/e2e-advisor/comment.mjs" "${COMMENT_ARGS[@]}"
          else
            echo "Skipping E2E advisor comment: trusted main checkout does not contain comment.mts or comment.mjs"
          fi

      - name: Post E2E target advisor PR comment
        if: ${{ always() && (github.event_name == 'pull_request' || github.event_name == 'workflow_run') }}
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.E2E_ADVISOR_GITHUB_TOKEN || github.token }}
          PR_NUMBER: ${{ github.event_name == 'workflow_run' && steps.fork_pr.outputs.pr_number || github.event.pull_request.number }}
        run: |
          if [ -f "$GITHUB_WORKSPACE/artifacts/e2e-advisor/e2e-target-advisor-summary.md" ] && [ -f "$ADVISOR_DIR/tools/e2e-advisor/target-comment.mts" ]; then
            node --experimental-strip-types "$ADVISOR_DIR/tools/e2e-advisor/target-comment.mts" \
              --repo "$GITHUB_REPOSITORY" \
              --pr "$PR_NUMBER" \
              --summary "$GITHUB_WORKSPACE/artifacts/e2e-advisor/e2e-target-advisor-summary.md" \
              --result "$GITHUB_WORKSPACE/artifacts/e2e-advisor/e2e-target-advisor-result.json"
          else
            echo "Skipping E2E target advisor comment: summary or target-comment.mts missing"
          fi

      - name: Upload advisor artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: e2e-advisor
          path: artifacts/e2e-advisor/
          if-no-files-found: warn
